version: "3.9"

services:
  site:
    build: .
    image: egon.io/jekyll:latest
    command: bundle exec jekyll serve --trace --host 0.0.0.0 --watch --incremental
    environment:
      - JEKYLL_ENV=development
    volumes:
      - ./docs/:/srv/jekyll
    ports:
      - 4000:4000